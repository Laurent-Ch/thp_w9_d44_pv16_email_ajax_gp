
document.getElementById("selected_content").innerHTML = "<%= j render 'selected', email: @email %>";

<!-- Front side -->
document.querySelector("a[href='/emails/<%= params[:id]%>']").setAttribute("class", "read_email");

<% @email.update(read: true) %>
